<Window x:Class="Styles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Styles"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.Resources>
            <Style x:Key="KalimStyle" TargetType="{x:Type TextBox}">
                <Setter Property="Background" Value="Magenta"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="LightGreen"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <TextBox Style="{StaticResource KalimStyle}" Height="23" Name="textBox1" Width="120"/>
        <TextBox Style="{StaticResource ResourceKey=KalimStyle}" Height="23" Name="textBox2" Width="120" Margin="192,70,191,381"/>

    </Grid>
</Window>
